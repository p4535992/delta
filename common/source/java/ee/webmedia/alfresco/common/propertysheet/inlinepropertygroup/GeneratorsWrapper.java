package ee.webmedia.alfresco.common.propertysheet.inlinepropertygroup;

/**
 * Marker interface that indicates that even if propertySheet is not in edit-mode, generator should be still called instead of generating simple outputtext
 * 
 * @author Ats Uiboupin
 */
public interface GeneratorsWrapper {
    // marker interface - no body
}
